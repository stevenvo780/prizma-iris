# IRIS Full Stack – DEV local
# DB: pgram en prizma2 (100.98.5.11:5432) via NetBird VPN
services:
  redis:
    image: redis:7-alpine
    container_name: iris-redis
    ports:
      - "6380:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - iris-network

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: iris-backend
    env_file: .env
    ports:
      - "3001:3001"
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - iris-network
    restart: unless-stopped

  frontend:
    build:
      context: ../iris-frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: http://localhost:3001/api
    container_name: iris-frontend
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:3001/api
      NEXT_PUBLIC_API_BASE_URL: http://localhost:3001
      NEXT_PUBLIC_WS_URL: ws://localhost:3001
      NEXT_PUBLIC_APP_NAME: IRIS - Enhanced Marketing WhatsApp
      NEXT_PUBLIC_VERSION: 2.0.0
      NODE_ENV: development
      NEXT_PUBLIC_DEBUG_MODE: "true"
    ports:
      - "3000:3000"
    depends_on:
      - backend
    networks:
      - iris-network
    restart: unless-stopped

networks:
  iris-network:
    driver: bridge
