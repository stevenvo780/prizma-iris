{
  "buildCommand": "cd ../../ && pnpm build:ds && cd apps/iris/emw-frontend && npm run build",
  "installCommand": "cd ../../ && pnpm install --frozen-lockfile",
  "env": {
    "NODE_ENV": "production"
  }
}
