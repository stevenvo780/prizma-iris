{
  "buildCommand": "npm run build",
  "installCommand": "npm install --legacy-peer-deps 2>&1 || npm install --legacy-peer-deps --no-save 2>&1 || node postinstall.js || true",
  "env": {
    "NODE_ENV": "production",
    "NEXT_SKIP_SWC_BINARY_VALIDATION": "1"
  }
}
